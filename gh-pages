# 在本地创建一个空的 gh-pages 分支
git checkout --orphan gh-pages
git rm -rf .
touch .gitignore
echo "# 部署分支" > README.md
git add .gitignore README.md
git commit -m "Initial gh-pages commit"
git push origin gh-pages
git checkout main
